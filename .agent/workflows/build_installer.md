---
description: Build the Windows Installer (EXE) for POS 2025
---

1. Analyze `package.json` to confirm the build script is `dist:exe`.
2. Run the build command.
// turbo
3. `npm run dist:exe`
4. Verify the output exists in `dist_exe`.
